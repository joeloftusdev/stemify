cmake_minimum_required(VERSION 3.14)

project(Stemify)

#Use if building with Visual Studio CMake
#SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/binaries/") 

add_subdirectory(SpleeterUI)


